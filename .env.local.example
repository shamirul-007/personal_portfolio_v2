DEV_TO_API_KEY=y7fiQcpBDdVTzNbmPic89PSFo
GITHUB_API_KEY=secret_hmVZxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxIXEQ
NEXT_PUBLIC_GITHUB_USERNAME = itsnitinr
NOTION_API_TOKEN =
NOTION_DATABASE_ID = c494cxxxxxxxxxxxxxxxxxxxxxx3b050

# Admin panel credentials (projects CRUD)
ADMIN_USERNAME = your-admin-username
ADMIN_PASSWORD = a-strong-password
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET = a-long-random-secret

# Supabase — required in production for project CRUD to persist.
# Provided by the Vercel Supabase integration (supabase-indigo-ocean).
# The service role key is server-only — never expose it to the client.
# Leave unset locally to use the JSON file fallback.
SUPABASE_URL =
SUPABASE_SERVICE_ROLE_KEY =